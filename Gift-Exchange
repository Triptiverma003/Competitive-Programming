#include<bits/stdc++.h>
using namespace std;
int main(){
	int n,p;
	cin>>n;
	vector<int>P(n+1);
	vector<int> result(n + 1);
	for(int i = 1; i<=n; i++){
		int gift_from;
		cin>>gift_from;
		result[gift_from] = i;
	}
	for(int i = 1; i<=n; i++){
		cout<<result[i] <<" ";
	}
	return 0;
}
