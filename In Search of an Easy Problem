#include <bits/stdc++.h>
using namespace std;
 
int main() {
	int t;
	cin >>t;
	int sum = 0;
	int n;
	while(t--){
		cin>>n;
		sum+=n;
	}
	if(sum >= 1){
			cout<<"Hard";
		}else{
			cout<<"Easy";
		}
	return 0;
}
